name: austerity
description: Dart and Flutter static code analysis, but not for the faint hearted. This code tries to turn all violations in to red squiggly lines
version: 0.0.9-beta
repository: https://github.com/MelbourneDeveloper/austerity

environment:
  sdk: '>=2.17.6 <3.0.0'

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
